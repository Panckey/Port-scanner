# Port-scanner

One of my first true project that work

You can use it by using

"python PortScanner.py -ip (DomainName or IP) -min (The port the scanner will start to, by default 1) -max (the port the scanner will end, by default 65535) -out (create an output file, optional)

Feel free to do anything with this
